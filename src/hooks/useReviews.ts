"use client";  import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query"; import { catalogService } from "@/services/catalog.service"; import type { Review } from "@/types";  export function useReviews(productId: string, page = 1, limit = 10) {   return useQuery({     queryKey: ["reviews", productId, page, limit],     queryFn: () => catalogService.getReviews(productId, page, limit),     enabled: !!productId,   }); }  export function useCreateReview() {   const queryClient = useQueryClient();    return useMutation({     mutationFn: (payload: { productId: string; rating: number; title?: string; comment?: string }) =>       catalogService.createReview(payload),     onSuccess: (_, variables) => {       queryClient.invalidateQueries({ queryKey: ["reviews", variables.productId] });     },   }); }